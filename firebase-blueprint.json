{
  "entities": {
    "Sale": {
      "title": "Sale",
      "description": "Represents a transaction of water depot sale.",
      "type": "object",
      "properties": {
        "amount": {
          "type": "number",
          "description": "Total amount of the sale in IDR"
        },
        "item": {
          "type": "string",
          "description": "Name or type of water / service sold"
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "ISO 8601 string of the creation time"
        },
        "paymentMethod": {
          "type": "string",
          "description": "Payment method used (e.g. Cash, QRIS)"
        },
        "userId": {
          "type": "string",
          "description": "The UID of the user who made the sale"
        }
      },
      "required": ["amount", "item", "createdAt", "paymentMethod", "userId"]
    }
  },
  "firestore": {
    "/sales/{saleId}": {
      "schema": "Sale",
      "description": "The collections containing sales logs of the user."
    }
  }
}
